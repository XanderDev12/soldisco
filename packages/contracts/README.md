# Contracts

Versioned event and API shapes shared by producers and consumers. Events carry
correlation, causation, and observation timestamps for replay and auditability.

The next contract milestone adds source/filter provenance, Solana and market
coordinates, candidate-window identity, immutable snapshot references, and
independent component status. See the [data lifecycle
model](../../docs/architecture/data-lifecycle.md).
