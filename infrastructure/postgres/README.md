# PostgreSQL plan

PostgreSQL is a possible ordinary relational store for append-only observations
and rebuildable projections, but it is not selected or configured.

Schema, migrations, retention, partitioning, backup, and recovery decisions are
deferred until observation identity, correction, replay, and projection
contracts are stable.
