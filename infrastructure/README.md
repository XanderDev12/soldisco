# Infrastructure

This directory reserves space for local development dependencies and deployment configuration.

Potential future areas:

- `postgres` — relational and time-series persistence
- `nats` — durable pipeline events and replay
- `telemetry` — logs, metrics, and traces

No containers or services are configured yet. Infrastructure should be introduced only when a milestone requires it and its operational ownership is clear.
