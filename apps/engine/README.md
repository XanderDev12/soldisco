# Engine

Planned home for normalization, deduplication, enrichment orchestration, deterministic first-pass checks, risk, ratings, momentum features, and strategy evaluation.

Every output must be explainable through reason codes, source timestamps, and engine versions. AI output may be attached as advisory context but cannot override deterministic results.

Axiom ingestion, RPC calls, and actual scam or rug rules are deferred.
